$(document).ready(function () {\n  // Event listener for click event on .todo elements\n  $(document).on('click', '.todo', function () {\n    $(this).toggleClass(\"done\");\n  });\n\n  // Event listener for submit event on .todo-form form\n  $(\".todo-form\").on('submit', function (e) {\n    e.preventDefault();\n    const todoInput = $(\'#todo-input\');\n\n    // Get the value from the todoInput textbox and create a \n    // new <li> element with that value\n    const todoText = todoInput.val();\n    const newTodo = $(`<li class=\\\"todo\\\">${todoText}</li>`);\n\n    // Add the new <li> element to the existing <ul>\n    $('#todo-list').append(newTodo);\n\n    // Clear the todoInput textbox\n    todoInput.val('');\n  });\n});